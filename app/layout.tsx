import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "The Boutique at Horton Place | Simple Luxury, Curated for You",
  description: "Experience the finest collection of handcrafted fashion, lifestyle essentials, and premium comfort in the heart of Colombo. A premium lifestyle destination where fashion meets elegance.",
  keywords: "boutique, fashion, luxury, Colombo, lifestyle, ethnic wear, linen collection, plus size fashion, 62 Precinct",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
